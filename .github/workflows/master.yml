name: Deploy Shopify Theme

on:
  push:
    branches:
      - master
jobs:
  shopify:
    name: Shopify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Shopify
        uses: pgrimaud/action-shopify@master
        env:
          SHOPIFY_PASSWORD: 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
          SHOPIFY_STORE_URL: test-webezy.myshopify.com
          SHOPIFY_THEME_ID: 122960478385
          THEME_PATH: ./
